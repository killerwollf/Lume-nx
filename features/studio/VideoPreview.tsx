'use client';
import {useRef,useEffect} from 'react';
export const demoVideo='https://interactive-examples.mdn.mozilla.net/media/cc0-videos/flower.webm';
export function VideoPreview({poster,auto=false,controls=false,src=demoVideo}:{poster?:string;auto?:boolean;controls?:boolean;src?:string}){const ref=useRef<HTMLVideoElement>(null);useEffect(()=>{const v=ref.current;if(!v)return;const observer=new IntersectionObserver(([e])=>{if(!e.isIntersecting)v.pause();else{if(!v.getAttribute('src'))v.src=src;if(auto&&!matchMedia('(prefers-reduced-motion: reduce)').matches)void v.play().catch(()=>{})}},{threshold:.25});observer.observe(v);if(!auto&&!controls)v.pause();return()=>observer.disconnect()},[auto,src,controls]);return <video ref={ref} poster={poster} muted loop playsInline controls={controls} preload="metadata" onPlay={()=>{document.querySelectorAll('video').forEach(v=>{if(v!==ref.current)v.pause()})}} onMouseEnter={()=>{if(!matchMedia('(prefers-reduced-motion: reduce)').matches)void ref.current?.play().catch(()=>{})}} onMouseLeave={()=>{if(!auto)ref.current?.pause()}}/>}
